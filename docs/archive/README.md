# Historical MPRO-era documentation

These files were copied from the former permanent `docs` branch at:

```text
3f23cfcfee6de4290a686f5ac7b1c69f23a03560
```

They are retained only for historical traceability and **must not be treated as current Form LOOQ documentation**. They contain older product names, identifiers, versions, URLs, and architectural assumptions.

Current documentation lives one level above this archive.

The old self-contained landing page was intentionally not copied into `main` because it is a large, obsolete MPRO/sayid.ir artifact. Its exact bytes remain recoverable from:

```text
backup/docs-branch-pre-consolidation-2026-09-07
```
